{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main SyncBlaze window",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-show", "core:window:allow-set-focus"],
  "_comment": "App-level #[tauri::command] functions registered via invoke_handler (get_lan_info) are callable under core:default in current Tauri v2 releases. If a future version tightens this, add its specific permission id here — check `cargo tauri dev`'s console for the exact permission it's asking for."
}
